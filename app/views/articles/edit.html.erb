<div class="edit-container">
  <%= form_for(@article, :html => { :class => 'input' }) do |f| %>
    <%= f.label :item, "Title", :class => 'input__label' %>
    <%= f.text_field :item, :class => 'input__title' %>
    <%= f.label :content, "Content", :class => 'input__label' %>
    <%= f.text_area :content, :class => 'input__content' %>
    <%= f.submit %>
  <% end %>
  <% if current_user && current_user.is_admin %>
    <a class="button"><%= link_to 'Delete', @article,  method: :delete, data: { confirm: 'Are you sure?' } %></a>
  <% end %>
  <%= link_to 'Back', :root %>
</div>
